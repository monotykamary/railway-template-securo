server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # API proxy to backend over Railway private networking.
    # Railway does not provide Docker's embedded DNS (127.0.0.11); use
    # public resolvers as the platform bridges internal DNS through them.
    resolver 8.8.8.8 8.8.4.4 valid=10s ipv6=off;
    set $upstream ${BACKEND_URL};
    location /api/ {
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location = /index.html {
        add_header Cache-Control "no-store, no-cache, must-revalidate";
        try_files $uri =404;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
